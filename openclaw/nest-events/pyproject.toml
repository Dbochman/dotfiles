[project]
name = "openclaw-nest-events"
version = "0.1.0"
description = "Locked runtime dependencies for the OpenClaw Nest SDM Pub/Sub listener"
requires-python = ">=3.11,<3.15"
dependencies = [
    "google-cloud-pubsub==2.37.0",
]

[tool.uv]
package = false
