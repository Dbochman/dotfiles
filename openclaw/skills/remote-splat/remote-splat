#!/usr/bin/env bash
# remote-splat — Guarded remote Gaussian-splat workflow on the RTX 5090 desktop
set -euo pipefail
SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
export PATH="/opt/homebrew/bin:/usr/bin:/bin"
exec /usr/bin/python3 "$SCRIPT_DIR/scripts/remote_splat.py" "$@"
