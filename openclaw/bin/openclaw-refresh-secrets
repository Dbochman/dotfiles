#!/bin/bash
# Refresh OpenClaw secrets cache from 1Password.
# Run this over SSH or interactively when op read works.
# Usage: ssh dylans-mac-mini ~/bin/openclaw-refresh-secrets
set -euo pipefail

export HOME="/Users/dbochman"
export PATH="/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin"
export OP_SERVICE_ACCOUNT_TOKEN=$(cat "$HOME/.openclaw/.env-token")

CACHE="$HOME/.openclaw/.secrets-cache"
TMP=$(mktemp)

echo "Fetching secrets from 1Password..."
{
  echo "OPENAI_API_KEY=$(/opt/homebrew/bin/op read 'op://OpenClaw/OpenAI API Key/password')"
  echo "ELEVENLABS_API_KEY=$(/opt/homebrew/bin/op read 'op://OpenClaw/ElevenLabs API Key/password')"
  echo "OPENCLAW_GATEWAY_TOKEN=$(/opt/homebrew/bin/op read 'op://OpenClaw/OpenClaw Gateway Token/password')"
  echo "BLUEBUBBLES_PASSWORD=$(/opt/homebrew/bin/op read 'op://OpenClaw/BlueBubbles Password/password')"
  echo "CIELO_USERNAME=$(/opt/homebrew/bin/op read 'op://OpenClaw/Cielo Login/username')"
  echo "CIELO_PASSWORD=$(/opt/homebrew/bin/op read 'op://OpenClaw/Cielo Login/password')"
  echo "STARMARKET_PASSWORD=$(/opt/homebrew/bin/op read 'op://OpenClaw/Star Market/password')"
  echo "STARMARKET_USERNAME=$(/opt/homebrew/bin/op read 'op://OpenClaw/Star Market/username')"
  # Static Star Market config (not in 1Password — these are account-specific identifiers, not rotatable secrets)
  echo "STARMARKET_GMAIL=julia.joy.jennings@gmail.com"
  echo "STARMARKET_USER_HASH=29fb95f3f8a30a41fb17ac7e74543649e5a69d54e423d2ba66a91d67a28918bb"
  echo "STARMARKET_DEVICE_TOKEN=0bbf660dcdd2bed236a204f4eaa54bb6"
} > "$TMP"

mv "$TMP" "$CACHE"
chmod 600 "$CACHE"
echo "Secrets cached to $CACHE ($(wc -c < "$CACHE") bytes)"
echo "Restart the gateway to pick up changes: launchctl stop ai.openclaw.gateway && launchctl start ai.openclaw.gateway"
